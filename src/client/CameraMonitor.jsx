import React from 'react';
import AxisCamera from './AxisCamera.jsx';

export default class CameraMonitor extends React.Component {
  render() {
    return (
      <div className="CameraMonitor"><AxisCamera /></div>
    );
  }
};
